{
  "name": "healthcare-website-enhancements-root",
  "private": true,
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:client",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}